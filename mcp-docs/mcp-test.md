# mcp-test MCP 服务命令指南

> 自动生成自 `mcp-test` MCP 服务端工具列表。

## 工具概览

| 工具名 | 描述 |
|--------|------|
| `get_weather` | 查询指定城市的天气信息。 |
| `list_cities` | 列出所有支持查询天气的城市。 |
| `add` | 计算两个数的和。 |
| `subtract` | 计算两个数的差。 |
| `multiply` | 计算两个数的积。 |
| `divide` | 计算两个数的商。 |

## 工具详情

### `get_weather`

**描述**：查询指定城市的天气信息。

**参数**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| `--city` | `string` | 是 | - | 城市名称，如：北京、上海、广州 |

**使用示例**：
```bash
mcp-cli --server mcp-test get_weather --city <city>
```

### `list_cities`

**描述**：列出所有支持查询天气的城市。

此工具无需参数。

**使用示例**：
```bash
mcp-cli --server mcp-test list_cities 
```

### `add`

**描述**：计算两个数的和。

**参数**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| `--a` | `number` | 是 | - | 第一个数 |
| `--b` | `number` | 是 | - | 第二个数 |

**使用示例**：
```bash
mcp-cli --server mcp-test add --a 3.14 --b 3.14
```

### `subtract`

**描述**：计算两个数的差。

**参数**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| `--a` | `number` | 是 | - | 被减数 |
| `--b` | `number` | 是 | - | 减数 |

**使用示例**：
```bash
mcp-cli --server mcp-test subtract --a 3.14 --b 3.14
```

### `multiply`

**描述**：计算两个数的积。

**参数**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| `--a` | `number` | 是 | - | 第一个因数 |
| `--b` | `number` | 是 | - | 第二个因数 |

**使用示例**：
```bash
mcp-cli --server mcp-test multiply --a 3.14 --b 3.14
```

### `divide`

**描述**：计算两个数的商。

**参数**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| `--a` | `number` | 是 | - | 被除数 |
| `--b` | `number` | 是 | - | 除数（不能为 0） |

**使用示例**：
```bash
mcp-cli --server mcp-test divide --a 3.14 --b 3.14
```
